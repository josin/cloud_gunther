# == Schema Information
# Table name: tasks
# Fields: id, started_at, finished_at, params, inputs, 
#         state, user_id, algorithm_binary_id, created_at, updated_at, 
#         cloud_engine_id, task_params, image_id, priority, attempts, 
#         error_msg, failed_at, #

task_1:
  started_at: <%= Time.now %>
  params: --debug
  inputs: /Users/deployer/.ssh
  state: new
  user_id: test_user1
  algorithm_binary_id: binary_1
  attempts: 0
