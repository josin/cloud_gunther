require 'spec_helper'

describe "tasks/show.html.erb" do
  # before(:each) do
  #   # @algorithm = assign(:algorithm, stub_model(Algorithm, :name => "Test alg"))
  #   # @algorithm = assign(:algorithm, stub_model(Algorithm, :name => "Test alg"))
  #   @algorithm_binary = stub_model(AlgorithmBinary, 
  #     :version => '1.0', 
  #     :algorithm => stub_model(Algorithm, :name => 'Test Alg')
  #   )
  #   @user = stub_model(User, :name => "John Doe")
  #   @task = assign(:task, stub_model(Task,
  #     :algorithm_binary => @algorithm_binary,
  #     :user => @user,
  #     :params => "MyText",
  #     :inputs => "MyText",
  #     :state => "State",
  #     :user_id => 1,
  #     :algorithm_binary_id => 1,
  #     :created_at => Time.now,
  #     :updated_at => Time.now
  #   ))
  #   
  #   @search = assign(:search, Output.metasearch)
  # end
  # 
  # it "renders attributes in <p>" do
  #   render
  #   # Run the generator again with the --webrat flag if you want to use webrat matchers
  #   rendered.should match(/MyText/)
  #   # Run the generator again with the --webrat flag if you want to use webrat matchers
  #   rendered.should match(/MyText/)
  #   # Run the generator again with the --webrat flag if you want to use webrat matchers
  #   rendered.should match(/State/)
  #   # Run the generator again with the --webrat flag if you want to use webrat matchers
  #   rendered.should match(/1/)
  #   # Run the generator again with the --webrat flag if you want to use webrat matchers
  #   rendered.should match(/1/)
  # end
end
